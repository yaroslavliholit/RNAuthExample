import React, {memo} from 'react';
import {SafeAreaView, Text} from 'react-native';

const UsersList = () => (
  <SafeAreaView>
    <Text>UsersList</Text>
  </SafeAreaView>
);

export default memo(UsersList);
